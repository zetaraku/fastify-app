{
  "name": "fastify-app",
  "version": "0.0.0",
  "description": "This project was bootstrapped with Fastify-CLI.",
  "main": "app.ts",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "node --test -r @swc-node/register test/**/*.test.ts",
    "start": "fastify start -r @swc-node/register -l info app.ts",
    "dev": "fastify start -r @swc-node/register -w -l info -P app.ts"
  },
  "keywords": [],
  "author": "Raku Zeta <zetaraku@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "fastify": "^4.26.1",
    "fastify-plugin": "^4.0.0",
    "@fastify/autoload": "^5.0.0",
    "@fastify/sensible": "^5.0.0",
    "fastify-cli": "^6.3.0"
  },
  "devDependencies": {
    "@swc-node/register": "^1.10.9"
  }
}
